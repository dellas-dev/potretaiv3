{
  "name": "potretai-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy untuk PotretAI — menyembunyikan Fal.ai API Key",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put FAL_API_KEY",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.73.0"
  }
}
